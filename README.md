# java-vityarthi-project
A Java CLI tool that encrypts and decrypts files using AES-256-GCM with PBKDF2 password-based key derivation. It provides authenticated encryption (confidentiality + tamper detection in one step), unique salt/IV per file, and a SHA-256 hashing utility for integrity checks — all using only the standard JDK, no external libraries.
